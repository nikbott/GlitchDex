# PostgreSQL Datasource Configuration for the 'docker' profile
spring.datasource.url=jdbc:postgresql://db:5432/glitchdex
spring.datasource.username=glitchdex
spring.datasource.password=password
# Hibernate will update the DB schema to match entities on startup
spring.jpa.hibernate.ddl-auto=update